/**
 * Bundled top npm package names for dependency provenance checks
 * (typosquat proximity + slopsquat borrowed-name detection).
 * Names only — never fetched at runtime.
 */
export declare const TOP_NPM_PACKAGE_NAMES: readonly string[];
export declare const TOP_NPM_PACKAGE_NAME_SET: ReadonlySet<string>;
